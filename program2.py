# maybe check the indexes in both strings and check for values.. even if one possible true, return true. use dp to keep track

def decode_message( s: str, p: str) -> bool:

        
  
        return False